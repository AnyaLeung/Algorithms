 //with queue:sque and dict:meeted
 //connect, length
 
start.enque();

 while(!sque.empty){
    pre = sque.deque();
    for i in pre.connected_queue{
        sque.enque();
        intable i.connected = pre; 
        length = 1;
    }
 }
